import {Box} from "@mui/material";
import theme from "../theme";

const Feed = () => {
  return (
    <Box bgcolor={theme.palette.primary.light} flex={4} p={2}>
      Feed
    </Box>
  );
}
export default Feed;